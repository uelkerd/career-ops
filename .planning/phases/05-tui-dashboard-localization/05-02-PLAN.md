---
phase: 05-tui-dashboard-localization
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - dashboard/internal/ui/screens/pipeline.go
  - dashboard/internal/ui/screens/viewer.go
  - dashboard/internal/ui/screens/progress.go
  - dashboard/internal/ui/screens/pipeline_test.go
  - dashboard/internal/ui/screens/viewer_test.go
  - dashboard/internal/ui/screens/timeago_test.go
  - dashboard/main.go
autonomous: true
requirements:
  - REQ-3.4
user_setup: []

must_haves:
  truths:
    - "Running `career-dashboard -lang=tr` or setting `CAREER_OPS_LANG=tr` initializes the dashboard in Turkish"
    - "Pressing 't' or 'T' in the TUI toggles the active language between English and Turkish at runtime without restarting or losing selection state"
    - "Table column headers, status badges, progress metrics, and footer shortcuts display localized strings from `i18n.Current`"
    - "Selecting a Turkish status in the overlay picker emits canonical English IDs in update messages so tracker files are never corrupted with localized text"
  artifacts:
    - dashboard/internal/ui/screens/pipeline.go
    - dashboard/internal/ui/screens/viewer.go
    - dashboard/internal/ui/screens/progress.go
    - dashboard/internal/ui/screens/pipeline_test.go
    - dashboard/internal/ui/screens/viewer_test.go
    - dashboard/main.go
  key_links:
    - "`main.go` imports `internal/i18n` and calls `i18n.SetLang` before starting Bubble Tea"
    - "`pipeline.go` uses `i18n.Current.StatusLabel(norm)` for display but emits canonical English IDs in `PipelineUpdateStatusMsg`"
    - "Table column width for status is adjusted to 15 runes so Turkish strings like `DEĞERLENDİRİLDİ` don't break layout or truncate"
---

<objective>
Localize all Go TUI Dashboard screens (`pipeline`, `viewer`, `progress`) to consume strings from `i18n.Current` and wire startup locale detection and runtime keyboard toggling into `main.go`.

Purpose: Provides full Turkish language support across the command-line dashboard UI while preserving strict data storage separation and terminal alignment integrity.
Output: Fully localized screen components and language detection wiring in `main.go` with passing unit and regression tests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tui-dashboard-localization/05-RESEARCH.md
@.planning/phases/05-tui-dashboard-localization/05-VALIDATION.md
@.planning/phases/05-tui-dashboard-localization/05-01-PLAN.md

@dashboard/internal/ui/screens/pipeline.go
@dashboard/internal/ui/screens/viewer.go
@dashboard/internal/ui/screens/progress.go
@dashboard/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 05-02-01: Localize TUI screens and adjust column width budgets (`ui/screens/...`)</name>
  <files>dashboard/internal/ui/screens/pipeline.go, dashboard/internal/ui/screens/viewer.go, dashboard/internal/ui/screens/progress.go, dashboard/internal/ui/screens/pipeline_test.go, dashboard/internal/ui/screens/viewer_test.go, dashboard/internal/ui/screens/timeago_test.go</files>
  <read_first>dashboard/internal/ui/screens/pipeline.go, dashboard/internal/ui/screens/viewer.go, dashboard/internal/ui/screens/progress.go, dashboard/internal/ui/screens/timeago_test.go</read_first>
  <action>
1. In `dashboard/internal/ui/screens/pipeline.go`:
   - Import `"github.com/santifer/career-ops/dashboard/internal/i18n"`.
   - Replace hardcoded English strings in table column headers (`FIT`, `DATE`, `COMPANY`, `ROLE`, `STATUS`, `LOCATION`, `PAY`, `LAST`), filter tab labels (`ALL`, `EVALUATED`, `APPLIED`, `INTERVIEW`, `TOP ≥4`, `SKIP`, `REJECTED`, `DISCARDED`), footer shortcut text, and empty state messages with references to `i18n.Current` fields.
   - Replace the implementation of `statusLabel(norm string) string` with `return i18n.Current.StatusLabel(norm)`.
   - Replace the implementation of `formatTimeAgo(dateStr string) string` with `return i18n.Current.FormatTimeAgo(dateStr)`.
   - In `func (m PipelineModel) columnWidths() colWidths`, increase the `status` column width budget from `12` to `15` (`c := colWidths{num: 5, score: 5, company: 16, status: 15}`) so uppercase Turkish status labels like `DEĞERLENDİRİLDİ` render without truncation or table border misalignment.
   - In `overlayStatusPicker`, ensure the options displayed to the user use `i18n.Current.StatusLabel(opt)`, but when emitting `PipelineUpdateStatusMsg.NewStatus`, pass the canonical English lowercase status ID (`"interview"`, `"offer"`, etc.) so tracker files remain canonical English on disk.
2. In `dashboard/internal/ui/screens/viewer.go`:
   - Import `"github.com/santifer/career-ops/dashboard/internal/i18n"`.
   - Replace preview labels (`Loc: `, `Pay: `, `Last contact: `, `Remote: `, `Outcome: `) and work mode display names (`Remote`, `RemoteFlex`, `Hybrid`, `Full`) with references to `i18n.Current` fields.
3. In `dashboard/internal/ui/screens/progress.go`:
   - Import `"github.com/santifer/career-ops/dashboard/internal/i18n"`.
   - Replace section titles (`SEARCH PROGRESS`, `Pipeline Funnel`, `Score Distribution`, `Conversion Rates`, `Weekly Activity`) and summary format strings with references to `i18n.Current` fields.
4. In tests (`pipeline_test.go`, `viewer_test.go`, `timeago_test.go`):
   - Update tests to verify dynamic string rendering and add test assertions checking UI output when `i18n.SetLang("tr")` is active.
   - Verify that rune truncation and string width checks continue passing cleanly with Turkish multi-byte characters (`ç`, `ğ`, `ı`, `İ`, `ö`, `ş`, `ü`).
  </action>
  <verify>cd dashboard && go test -v ./internal/ui/...</verify>
  <acceptance_criteria>
    - `dashboard/internal/ui/screens/pipeline.go` contains `i18n.Current`
    - `dashboard/internal/ui/screens/viewer.go` contains `i18n.Current`
    - `dashboard/internal/ui/screens/progress.go` contains `i18n.Current`
    - `dashboard/internal/ui/screens/pipeline.go` contains `status: 15`
    - `dashboard/internal/ui/screens/pipeline.go` contains `return i18n.Current.StatusLabel(norm)`
    - `dashboard/internal/ui/screens/pipeline.go` contains `return i18n.Current.FormatTimeAgo(dateStr)`
  </acceptance_criteria>
  <done>All unit and integration tests in `dashboard/internal/ui/...` pass with 0 errors.</done>
</task>

<task type="auto">
  <name>Task 05-02-02: Wire startup locale resolution and runtime keyboard toggle (`main.go`)</name>
  <files>dashboard/main.go</files>
  <read_first>dashboard/main.go</read_first>
  <action>
1. In `dashboard/main.go`, import `"github.com/santifer/career-ops/dashboard/internal/i18n"`.
2. Add a CLI string flag `-lang` (`var langFlag string; flag.StringVar(&langFlag, "lang", "", "UI language (en, tr)")`).
3. Implement a startup locale resolution helper before initializing Bubble Tea:
   - Priority 1: Check `langFlag` (if non-empty, use it).
   - Priority 2: Inspect environment variables `CAREER_OPS_LANG`, `LANG`, and `LC_ALL` (looking for substrings `"tr"` or `"en"`).
   - Priority 3: Check if `filepath.Join(careerOpsPath, "config/profile.yml")` exists. If so, read or inspect if `language.modes_dir` is set to `modes/tr` or `tr`.
   - Fallback: Default to `"en"`.
   - Call `i18n.SetLang(resolvedLang)`.
4. In `func (m appModel) Update(msg tea.Msg) (tea.Model, tea.Cmd)` in `main.go`, intercept keyboard events (`msg := msg.(type)` -> `case tea.KeyMsg:`):
   - Check if `msg.String() == "t" || msg.String() == "T"`.
   - If pressed, call `i18n.ToggleLang()` and return `m, nil` (force a re-render of the active view on the next frame without modifying scroll offset or selected index).
  </action>
  <verify>cd dashboard && go test -v ./...</verify>
  <acceptance_criteria>
    - `dashboard/main.go` contains `flag.StringVar(&langFlag, "lang"`
    - `dashboard/main.go` contains `i18n.SetLang(`
    - `dashboard/main.go` contains `i18n.ToggleLang()`
    - `dashboard/main.go` contains `CAREER_OPS_LANG`
    - `dashboard/main.go` contains `msg.String() == "t" || msg.String() == "T"`
  </acceptance_criteria>
  <done>Full test suite across `dashboard/...` passes cleanly and application builds without error.</done>
</task>

</tasks>
